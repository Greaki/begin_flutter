// new_post_page.dart
import 'package:flutter/material.dart';

class NewPostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('新增Post页面'),
      ),
      body: Center(
        child: Text('这是新增Post页面'),
      ),
    );
  }
}
